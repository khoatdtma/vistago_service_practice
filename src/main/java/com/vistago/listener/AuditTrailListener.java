package com.vistago.listener;

import org.hibernate.envers.RevisionListener;

public class AuditTrailListener implements RevisionListener {
    @Override
    public void newRevision(Object o) {

    }
}
